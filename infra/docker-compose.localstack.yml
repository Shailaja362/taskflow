services:
  localstack:
    image: localstack/localstack:3.8
    ports:
      - "4566:4566"
    environment:
      SERVICES: ec2,ecs,elbv2,iam,logs,rds,s3,sts,cloudwatch
      DEBUG: 0
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
